
class MeshGenerator {
  protected:
  public:
    MeshGenerator(const double &hight, const double &width,
                  const double &tickness_s, const double &tickness_g,
                  const double &radius, const double &length);
};